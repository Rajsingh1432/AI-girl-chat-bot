services:
  - type: web
    name: ai-girl-bot
    runtime: docker   # Render will build and run the Dockerfile itself
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
